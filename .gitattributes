# Ensure shell scripts always use LF line endings on all platforms
*.sh text eol=lf

# Gradle wrapper scripts should use LF
gradlew text eol=lf

# Windows batch files should use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# Let Git handle text files automatically
* text=auto
